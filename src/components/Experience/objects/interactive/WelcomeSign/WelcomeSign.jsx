import { Model } from "./WelcomeSignModel"

export const WelcomeSign = ({setMapHovered, setPubHovered}) => {
    

  return <Model position={[5, -6, -40]} rotation={[0, -Math.PI / 2, 0]} {...props} />
}
